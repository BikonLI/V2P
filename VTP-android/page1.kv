FloatLayout: 
    pos: root.pos

    canvas.before:
        Color: 
            rgba: 0.95, 0.96, 0.96, 1
        Rectangle:
            size: self.size
            pos: self.pos

        Color:
            rgba: 0.2, 0.6, 0.85, 1
        Line:
            points: 15, self.size[1] * 0.89, self.size[0] - 15, self.size[1] * 0.89
            width: 2.2

    Label:
        size_hint: 0.5, 0.05
        pos_hint: {"top": 0.95, "left": 1}
        color: 0, 0, 0, 1
        text: "[size=50]视频帧提取[/size]"
        markup: True

    Label: 
        size_hint: 1, 0.30
        pos_hint: {"top": 0.85, "left": 1}
        color: 0, 0, 0, 1
        text:
            """
            本应用可以将一段视频的帧提取为照片。
            在选取完视频文件之后，你需要设置四
            个属性，采样率、图片大小、图片格式、
            图片旋转。
            \n
            输出的图片会存放在/Documents/Ext
            ractPictures文件夹中。
            \n
            应用需要获取读写外部存储权限，和读
            取媒体权限。如果你选择了一个视频，
            但是依然提示请选择视频文件，那你需
            要检查该视频文件是否存储在云端。如
            果存储在云端，你需要手动下载之后再
            重新选择该视频文件。
            \n
            本应用开源：https://github.com/BikonLI/V2P"""
